# using gradle as build tool ..
image: qlik/gradle
 
pipelines:
  default:
    - step:
        script:
          - cd ./server
          - ./gradlew clean
          - ./gradle clean build buildFrontend